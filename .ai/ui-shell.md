# Android Dashboard UI shell (v0)

Goal: make the `android-dashboard` WebView wrapper feel like a real app:

- Top toolbar with title + back + refresh
- Visible loading state (progress + spinner)
- Friendly error state with retry

This is intentionally minimal and stays compatible with the current AppCompat setup (no Compose migration yet).

